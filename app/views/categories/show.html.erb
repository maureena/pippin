<h1><%= @category.name %></h1>

<%= link_to "Edit", edit_category_path(@category), class: '' %>

  <% if @subcategories %>
    <% @subcategories.each do |subcategory| %>
      <h4><%= link_to subcategory.name, subcategory %></h4>
    <% end %>
  <% end %>